# A1_Neural_Texture_Compression
Neural Graphics Assignment 1 - Implements both classic and neural method of texture compression


We are using top left corner as (0,0)

intall all python requirements by running
"pip install -r requirements.txt"